# -*- coding: utf-8 -*-

# TODO: should contain timers and manage them
# generic start/stop/pause/resume (for all contents)
class BaseTimerManager(object):
    pass

# should have at least start/stop/pause/resume
# attributes: interval, func (only one func! it can call others if needed)
class BaseTimer(object):
    pass
